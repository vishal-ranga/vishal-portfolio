apiVersion: v1
kind: ConfigMap
metadata:
  name: portfolio-config
  namespace: portfolio
data:
  NODE_ENV: "production"
  PORT: "3000"
  # Add any non-secret runtime configuration here, e.g.:
  # NEXT_PUBLIC_SITE_URL: "https://vishalranga.dev"
